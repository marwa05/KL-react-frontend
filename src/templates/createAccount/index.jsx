import Form from "./Form";
import PageTemplate from "../../components/pageTemplate";

const CreateAccount = () => {
  return (
    <PageTemplate data-testid="createAccountPage">
      <Form />
    </PageTemplate>
  );
};

export default CreateAccount;
